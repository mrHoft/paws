{
  "name": "fast-paws",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000",
    "build": "tsc && vite build",
    "deploy": "npm run build && git checkout gh-pages && rm -f main-*.js && rm -f styles-*.css && cp -r dist/* . && git add . && git commit -m 'feat: deploy' && git push"
  },
  "devDependencies": {
    "@types/node": "^24.6.2",
    "typescript": "~5.9.3",
    "vite": "^7.1.7"
  }
}
