{
  "name": "nimble-paws",
  "private": true,
  "version": "0.5.20",
  "type": "module",
  "main": "main.js",
  "description": "A nimble cat on an endless adventure game",
  "author": "mrHoft <ru.hoft@gmail.com>",
  "scripts": {
    "start": "electron .",
    "dev": "vite --port 3000",
    "build": "tsc && vite build",
    "deploy": "npm run build && git checkout gh-pages && rm -f engine-*.js && rm -f main-*.js && rm -f main-*.css && cp -r dist/* . && git add . && git commit -m 'feat: deploy' && git push",
    "make": "electron-builder --win"
  },
  "devDependencies": {
    "@types/node": "^24.6.2",
    "electron": "^39.2.6",
    "electron-builder": "^26.0.12",
    "typescript": "~5.9.3",
    "vite": "^7.1.7"
  },
  "build": {
    "appId": "nimble.paws",
    "icon": "dist/icons/icon",
    "win": {
      "target": [
        "portable"
      ]
    },
    "directories": {
      "output": "release"
    }
  }
}
